# Training Data Placeholder

This folder is reserved for dataset metadata and local data preparation artifacts.

Current repository state:

- No dataset is committed.
- Runtime engine is currently stub-based and does not consume this folder.

When training is enabled, document:

- data source
- label taxonomy
- train/validation/test split policy
- privacy and storage constraints
